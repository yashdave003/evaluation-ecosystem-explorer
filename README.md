# AI Evaluation Ecosystem (evaluarium) preview

Unlisted preview snapshot of the evaluarium site: the ecosystem map with its literature and events strip, the simulation explainer, and the run explorer. Every page carries `noindex, nofollow` and `robots.txt` disallows crawling.

This is a byte-for-byte copy of `website/client/` from the simulation repository, regenerated from there; do not edit here.
